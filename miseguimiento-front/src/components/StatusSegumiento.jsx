
// Componente reutilizable que muestra el estado de un producto
const StatusSegumiento = ({ statusString }) => {
    return (
        <div>
            <span>{statusString}</span>
        </div>
    )
}

export default StatusSegumiento
